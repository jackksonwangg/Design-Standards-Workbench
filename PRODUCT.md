# 基调

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

使用 CodeBuddy 等编码工具创建前端，但缺少统一设计规范意识的开发者。

## Product Purpose

通过组合和调整配色、字体、布局与组件，建立可执行的设计规范，一键导出 Markdown。

## Capabilities and Constraints

用户直接在配色、字体、布局、组件、数据可视化五个模块完成设计选择；系统将细则推导为 Token、页面规则和强制 Agent 指令。提供 16 套清爽界面配色、12 套图表色板、模拟后台页面试色、图标/指标卡/图表高级规则、业务看板/组件/内容/数据/一致性测试预览、规范健康度与 Markdown 下载。高级规则覆盖图表容量、长标签降级、表格行高、页面网格、无权限等中台状态；它们不会在首次使用时以零散参数打断选择。导出的 DESIGN_SYSTEM.md 同时包含给人的指南、给代码的 Token 和给 Agent 的强制变更门禁。预览与 Markdown 使用同一配置。参考截图是问题样本，不复刻业务数据；预览数据需明确标注为演示。

## Open Decisions

暂定名称“基调”；第一版默认直接进入工作台。配置作为本机偏好保存在浏览器。均为实现假设，可继续调整。

## Product Principles

先说明用途，再选择数值。每个值在预览与导出中一致。每次改动先读规范、只复用已有 Token 或组件；缺规则时先补规范。鼓励语义复用，不鼓励逐页面随意造样式。
