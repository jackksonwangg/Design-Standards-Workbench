# 基调工作台

采用排版校样台的秩序：固定中性工作台与独立可编辑画布。深墨色导航与轻灰画布，绿色仅用于当前选中方案。颜色样本是主要视觉材料，不使用装饰插图。

工作台字体为 Geist / 中文系统黑体；正文 14–16px，次要元信息 12px；主操作墨黑。控件边框 1px，圆角 6–8px。侧栏、编辑区、预览画布三个区域有明确分隔。移动端堆叠。

用户配置通过 lib/design.ts 统一派生，预览与导出引用同一源。工作台自身样式独立于用户配置。

配色灵感提供 16 套界面方案，以干净的白色容器、轻微染色的页面背景和清晰中性色为共同骨架，避免低明度、过度掺灰的主色污染内容层级。灵感页面采用“方案列表 + 页面试色”结构；点击方案后，模拟后台页面通过同一套 Token 同步更新导航、页面背景、卡片、文字、主按钮、状态色和图表。旧版预设草稿自动迁移到气质相近的新版方案，自定义草稿保持原值。

数据可视化作为第五类设计基础，沿用同一编辑器结构。设置区提供 12 套六色图表色板与六色自定义，色板按分类、顺序、分歧和重点强调场景组织，并全局控制标题、标签、坐标轴字号、卡片内边距、图例位置和网格线策略；颜色与卡片背景不足 3:1 时即时提示。预览区在同一张图表卡片中切换柱状、折线、饼状和桑基图，每种图形都附带无障碍数据表。演示数据必须显式标识，预览与 Markdown 导出始终引用同一份配置。

交付治理不作为第六个配置分类，而是由规范健康度和导出文档承接。用户先做少量影响全局的设计决策，系统再生成 8/12 列网格、桌面内容宽度、表格行高和动效规则。设计规范应先于代码：每次页面、组件、颜色、字体、布局或图表改动，先读取 DESIGN_SYSTEM.md 并写清范围、复用的 Token/组件和验收项；没有现成规则时，先补充规范与预览，再实现。列表页必须定义排序、筛选、分页和溢出处理，异步页面必须覆盖加载、空数据、错误、无权限和危险操作后的可撤销反馈。交付时报告响应式、状态反馈、键盘焦点、对比度与数据口径的检查结果。

组件页必须把图标与数据卡片作为一级规范。图标配置声明唯一来源（Lucide / Phosphor / Heroicons）、线性或面性风格、16/20/24px 尺寸和线性描边；同一页面不混用图标库或风格，图标按钮保留可访问名称和足够点击热区。状态色固定映射为主色=信息/进行中/选中，success=成功，warning=待处理/临期，danger=错误/阻断，muted=停用/未知，并同时使用文字或图标表达。

数据卡片明确 Header、Content、可选 Footer 的结构，标题最多两行，核心指标字号和标题区高度由配置生成。图表规范必须输出绘图区基准高度、最大可见类别、柱间距和长标签策略；响应式顺序为完整显示、两行换行、缩写并展示完整 Tooltip、间隔抽样、卡片内滚动或切换图形。柱状图、折线图、环形图和桑基图分别限制可见数量、尺寸与异常处理，不通过压缩字体无限塞入数据。右侧“一致性测试”用列表、表单、空数据和风险反馈检查同一套规则是否成立。
